<div
  class="blobs pointer-events-none fixed inset-0 z-[-1] overflow-clip"
  aria-hidden="true"
>
  <span
    style="--blob-core-size:7px"
    class="blobs-item blobs-item-top absolute top-[12%] left-1/2 size-115 rounded-full will-change-transform"
  ></span>
  <span
    style="--blob-core-size:6px"
    class="blobs-item blobs-item-left absolute top-[84%] left-[6%] size-85 rounded-full will-change-transform"
  ></span>
  <span
    style="--blob-core-size:5px"
    class="blobs-item blobs-item-right absolute top-[94%] left-[94%] size-77.5 rounded-full will-change-transform"
  ></span>
</div>

<style>
  .blobs-item::before {
    position: absolute;
    top: 50%;
    left: 50%;
    margin: calc(var(--blob-core-size) / -2) 0 0
      calc(var(--blob-core-size) / -2);
    box-shadow:
      0 0 8px rgba(255, 255, 255, 0.95),
      0 0 18px rgba(212, 212, 212, 0.6);
    border-radius: 50%;
    background: #fff;
    width: var(--blob-core-size);
    height: var(--blob-core-size);
    content: '';
  }

  .blobs-item-top {
    animation: blobs-drift-top 47s ease-in-out -9s infinite;
    background: radial-gradient(
      closest-side,
      rgba(163, 163, 163, 0.25),
      transparent 70%
    );
  }

  .blobs-item-left {
    animation: blobs-drift-left 61s ease-in-out -23s infinite;
    background: radial-gradient(
      closest-side,
      rgba(163, 163, 163, 0.15),
      transparent 70%
    );
  }

  .blobs-item-right {
    animation: blobs-drift-right 73s ease-in-out -41s infinite;
    background: radial-gradient(
      closest-side,
      rgba(163, 163, 163, 0.1),
      transparent 70%
    );
  }

  @keyframes blobs-drift-top {
    0%,
    100% {
      transform: translate(-50%, -50%) scale(1);
    }
    25% {
      transform: translate(calc(-50% + 9vw), calc(-50% + 5vh)) scale(1.09);
    }
    50% {
      transform: translate(calc(-50% - 7vw), calc(-50% + 9vh)) scale(0.95);
    }
    75% {
      transform: translate(calc(-50% + 4vw), calc(-50% - 6vh)) scale(1.04);
    }
  }

  @keyframes blobs-drift-left {
    0%,
    100% {
      transform: translate(-50%, -50%) scale(1);
    }
    33% {
      transform: translate(calc(-50% + 13vw), calc(-50% - 8vh)) scale(1.12);
    }
    66% {
      transform: translate(calc(-50% + 5vw), calc(-50% + 7vh)) scale(0.92);
    }
  }

  @keyframes blobs-drift-right {
    0%,
    100% {
      transform: translate(-50%, -50%) scale(1);
    }
    30% {
      transform: translate(calc(-50% - 11vw), calc(-50% - 5vh)) scale(0.94);
    }
    70% {
      transform: translate(calc(-50% - 4vw), calc(-50% - 12vh)) scale(1.1);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .blobs-item {
      transform: translate(-50%, -50%);
      animation: none;
    }
  }
</style>
